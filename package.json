{
  "name": "ferro",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "icons": "node scripts/build-icons.mjs",
    "dev": "npm run icons && hugo server --disableFastRender --buildDrafts",
    "build": "npm run icons && hugo --minify --gc"
  },
  "devDependencies": {
    "@tabler/icons": "^3.46.0",
    "@tailwindcss/cli": "^4.3.3",
    "@tailwindcss/typography": "^0.5.20",
    "prettier": "^3.9.5",
    "prettier-plugin-go-template": "^0.0.15",
    "prettier-plugin-tailwindcss": "^0.8.1",
    "tailwindcss": "^4.3.3"
  },
  "allowScripts": {
    "@parcel/watcher@2.5.1": true
  }
}
